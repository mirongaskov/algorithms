ПЕРЕД ЗАПУСКОМ НУЖНО В ДИРЕКТОРИИ С generate_inputs.py СОЗДАТЬ ПАПКУ 'files' С ПАПКАМИ 'outputs', 'inputs', 'answers'!


Сначала запускаем generate_inputs.py. Он генерирует файлы для тестов.
После запускаем main.cpp. Он записывает ответы в outputs.
В нем нужно изменить directory!
Затем уже запускаем check_answers_with_outputs.py. Он сверяет ответы с результатами полученными из main.cpp.
Если все OK, то все хорошо. Если есть хоть один WRONG ANSWER, то алгоритмы неверны.
